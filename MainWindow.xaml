<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CodexNamedMetaTagManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodexNamedMetaTagManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="NamedMetaTagManager">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid RowDefinitions="Auto,Auto,Auto,*" Padding="12" RowSpacing="12">
        <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBox x:Name="TagNameInput" Width="160" PlaceholderText="Metatag name" Text="important"/>
                <Button Content="Add Tag" Click="OnAddTagClicked"/>
                <Button Content="Remove Tag" Click="OnRemoveTagClicked"/>
                <Button Content="Hide Tag" Click="OnHideTagClicked"/>
                <Button Content="Show Tag" Click="OnShowTagClicked"/>
                <Button Content="Highlight Tag" Click="OnHiliteTagClicked"/>
                <Button Content="Clear Highlight" Click="OnClearHiliteTagClicked"/>
                <Button Content="List Tags" Click="OnListTagsClicked"/>
                <Button Content="List Tags (Detailed)" Click="OnListTagsWithDetailsClicked"/>
                <Button Content="Check Tag" Click="OnCheckTagClicked"/>
                <Button Content="Get Tag Content" Click="OnGetContentClicked"/>
                <Button Content="Add colour checkbox" Click="OnAddColourCheckBoxClicked"/>
            </StackPanel>
        </ScrollViewer>

        <TextBlock Grid.Row="1" x:Name="StatusText" Text="Ready" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" Height="30">
            <TextBlock Text="Color checkboxes:" VerticalAlignment="Center"/>
            <local:ColorCheckBoxesComponent x:Name="ColorCheckboxes" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Spacing="8">
            <TextBlock Text="RichEditBox with sample tags" Style="{StaticResource BodyStrongTextBlockStyle}"/>
            <RichEditBox x:Name="Editor" AcceptsReturn="True" VerticalAlignment="Stretch"
                         IsSpellCheckEnabled="False" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
